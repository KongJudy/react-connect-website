/*
const abuse = posts.filter((post) => post.topic === 'abuse');
const adhd = posts.filter((post) => post.topic === 'adhd');
const anxiety = posts.filter((post) => post.topic === 'anxiety');
const autism_spectrum = posts.filter((post) => post.topic === 'autism spectrum');autism_spectrum
const bipolar = posts.filter((post) => post.topic === 'bipolar');
const body_image = posts.filter((post) => post.topic === 'body image');
const borderline_personality = posts.filter((post) => post.topic === 'borderline personality');
const depression = posts.filter((post) => post.topic === 'depression');
const dissociation = posts.filter((post) => post.topic === 'dissociation');
const eating = posts.filter((post) => post.topic === 'eating');
const hearing_voices = posts.filter((post) => post.topic === 'hearing voices');
const hoarding = posts.filter((post) => post.topic === 'hoarding');
const hypomania = posts.filter((post) => post.topic === 'hypomania');
const loneliness = posts.filter((post) => post.topic === 'loneliness');
const ocd = posts.filter((post) => post.topic === 'ocd');
const paranoia = posts.filter((post) => post.topic === 'paranoia');
const personality = posts.filter((post) => post.topic === 'personality');
const phobias = posts.filter((post) => post.topic === 'phobias');
const postnatal_perinatal = posts.filter((post) => post.topic === 'postnatal/perinatal');
const ptsd = posts.filter((post) => post.topic === 'ptsd');
const pmdd = posts.filter((post) => post.topic === 'pmdd');
const psychosis = posts.filter((post) => post.topic === 'psychosis');
const addictions = posts.filter((post) => post.topic === 'addictions');
const schizoaffective = posts.filter((post) => post.topic === 'schizoaffective');
const schizophrenia = posts.filter((post) => post.topic === 'schizophrenia');
const seasonal_affective = posts.filter((post) => post.topic === 'seasonal affective');
const self_esteem = posts.filter((post) => post.topic === 'self esteem');
const self_harm = posts.filter((post) => post.topic === 'self harm');
const sleep = posts.filter((post) => post.topic === 'sleep issues');
const stress = posts.filter((post) => post.topic === 'stress');
const suicidal_feelings = posts.filter((post) => post.topic === 'suicidal feelings');
const tardive_dyskinesia = posts.filter((post) => post.topic === 'tardive dyskinesia');
const trauma = posts.filter((post) => post.topic === 'trauma');

*/
